@page "/old-home"
@inject NavigationManager NavManager

@code {
    protected override void OnInitialized()
    {
        // Automatically redirects the home layout scope directly into our twin engine dashboard
        NavManager.NavigateTo("dashboard");
    }
}